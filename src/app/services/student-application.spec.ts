import { StudentApplication } from "../model/student-application.model";

describe('StudentApplication', () => {
  it('should create an instance', () => {
    expect(new StudentApplication()).toBeTruthy();
  });
});
